Validation ops live in `docs/product_validation/`. Strategy SOT remains in `docs/product_strategy/`.

Executable PUB-I tools: `tools/customer_validation/` (empty workspace; counters start at 0).
Hard bans: `NEXUS_PUB_I_HARD_BANS_V1.md`.
