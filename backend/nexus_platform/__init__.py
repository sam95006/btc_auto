"""NEXUS platform product-architecture foundation (NEXUS-EXPERIENCE-1A).

Canonical, backend-owned CONTRACTS shared across surfaces: commercial plans +
prices + trial, the entitlement capability registry (with honest availability
states), the presentation VIEW-MODE contract (never an authorization boundary),
the data-licensing registry, and the logical data-domain / service-boundary map.

These are governance definitions. They align with (do not replace) the existing
``nexus_billing`` plan catalog; pricing/branding is metadata, plan ``code`` is
identity, and authorization is always backend entitlements — never frontend view
density.
"""
