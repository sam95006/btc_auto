# Demo config pointers (non-secret)

- Gate template (committed, no API keys): `../../deploy/zeabur_bybit_demo_validation/demo_founder_gate.env`
- Env example: `../../deploy/zeabur_bybit_demo_validation/.env.example`

Required secrets stay in GitHub Secrets / Zeabur Environment Variables only.
