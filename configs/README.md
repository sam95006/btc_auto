# Configs (target namespace)

**Phase A pointer only.**

Authoritative runtime config today: repository-root **`config/`**.

| Target (Phase B) | Current location |
|------------------|------------------|
| `configs/research/` | research config fragments under `config/` / tools |
| `configs/demo/` | `deploy/zeabur_bybit_demo_validation/demo_founder_gate.env` (non-secret gates) |
| `configs/testnet/` | testnet templates |
| `configs/templates/` | `configs/templates/` + `.env.example` files |

See `configs/demo/` and `configs/templates/` for Phase A stubs.
