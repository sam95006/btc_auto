/// Dart-side smoke notes. Full executable suite lives under tests/public_mobile_notify (Python).
/// Flutter/Dart SDK is optional on agent hosts; do not claim dart test PASS without toolchain.
void main() {
  // Intentionally empty placeholder so the package layout is complete.
}
