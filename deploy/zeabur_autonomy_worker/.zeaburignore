# Autonomy worker Zeabur upload exclusions (optional package root).
# Prefer building from repo root with Dockerfile.autonomy + root .zeaburignore
# (which un-ignores tools/research/activity_metric_v2 for V30).

.env
.env.*
*.secret
docs/
tests/
.pytest_cache/
data/
logs/
*.log
