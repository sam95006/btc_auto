diff --git a/cmd/main.go b/cmd/main.go
index da5c524..ae24788 100644
--- a/cmd/main.go
+++ b/cmd/main.go
@@ -40,6 +40,7 @@ func main() {
 			factory.Log = log.NewInfoLevel()
 		}
 		factory.Log.Error(err)
+		os.Exit(1)
 	}
 }
 
diff --git a/internal/cmd/deploy/deploy.go b/internal/cmd/deploy/deploy.go
index 998c8b5..4800671 100644
--- a/internal/cmd/deploy/deploy.go
+++ b/internal/cmd/deploy/deploy.go
@@ -3,6 +3,7 @@ package deploy
 import (
 	"context"
 	"fmt"
+	"os"
 
 	"github.com/briandowns/spinner"
 	"github.com/spf13/cobra"
@@ -27,6 +28,26 @@ type Options struct {
 	environmentID string
 }
 
+func emitDeployDiagnostic(key, value string) {
+	fmt.Fprintf(os.Stderr, "%s=%s\n", key, value)
+}
+
+func explicitEnvironment(environmentID, projectID string) *model.Environment {
+	return &model.Environment{
+		ID:        environmentID,
+		ProjectID: projectID,
+	}
+}
+
+func explicitService(serviceID, projectID string) *model.Service {
+	return &model.Service{
+		ID: serviceID,
+		Project: &struct {
+			ID string `graphql:"_id"`
+		}{ID: projectID},
+	}
+}
+
 func NewCmdDeploy(f *cmdutil.Factory) *cobra.Command {
 	opts := &Options{}
 
@@ -40,7 +61,7 @@ func NewCmdDeploy(f *cmdutil.Factory) *cobra.Command {
 
 	cmd.Flags().StringVar(&opts.projectID, "project-id", "", "Project ID to deploy on")
 	cmd.Flags().StringVar(&opts.serviceID, "service-id", "", "Service ID to redeploy on")
-	cmd.Flags().StringVar(&opts.environmentID, "environment-id", "", "Environment ID to redeploy on")
+	cmd.Flags().StringVar(&opts.environmentID, "environment-id", "", "Environment ID to deploy on")
 	cmd.Flags().StringVar(&opts.name, "name", "", "Service name")
 	cmd.Flags().StringVar(&opts.domainName, "domain", "", "Domain name")
 	cmd.Flags().BoolVar(&opts.create, "create", false, "Create a new service")
@@ -54,66 +75,71 @@ func runDeploy(f *cmdutil.Factory, opts *Options) error {
 	var projectID string
 	var err error
 
+	emitDeployDiagnostic("P2_DEPLOY_STAGE", "pack_zip")
 	bytes, fileName, err := util.PackZip()
 	if err != nil {
+		emitDeployDiagnostic("P2_DEPLOY_PACK_ZIP_PASS", "false")
+		emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
 		return fmt.Errorf("packing zip: %w", err)
 	}
+	emitDeployDiagnostic("P2_DEPLOY_PACK_ZIP_PASS", "true")
 	if opts.name == "" {
 		opts.name = fileName
 	}
 
 	if opts.projectID != "" {
-		// non-interactive mode: --project-id is provided
 		projectID = opts.projectID
-
-		if opts.serviceID != "" {
-			// redeploy to existing service
-			service, err = f.ApiClient.GetService(context.Background(), opts.serviceID, "", "", "")
-			if err != nil {
-				return err
-			}
+		emitDeployDiagnostic("P2_DEPLOY_STAGE", "resolve_targets")
+
+		if opts.serviceID != "" && opts.environmentID != "" {
+			emitDeployDiagnostic("P2_DEPLOY_GRAPHQL_SERVICE_LOOKUP_SKIPPED", "true")
+			emitDeployDiagnostic("P2_DEPLOY_GRAPHQL_ENV_LOOKUP_SKIPPED", "true")
+			emitDeployDiagnostic("P2_DEPLOY_SERVICE_ID_EXPLICIT", opts.serviceID)
+			emitDeployDiagnostic("P2_DEPLOY_ENVIRONMENT_ID_EXPLICIT", opts.environmentID)
+			service = explicitService(opts.serviceID, projectID)
+			environment = explicitEnvironment(opts.environmentID, projectID)
+		} else if opts.serviceID != "" {
+			emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
+			return fmt.Errorf("environment-id required when service-id is explicit")
 		} else {
-			// create a new service
+			emitDeployDiagnostic("P2_DEPLOY_STAGE", "create_service")
 			service, err = f.ApiClient.CreateEmptyService(context.Background(), projectID, opts.name)
 			if err != nil {
+				emitDeployDiagnostic("P2_DEPLOY_CREATE_SERVICE_PASS", "false")
+				emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
 				return err
 			}
-		}
-
-		if opts.environmentID != "" {
-			environment, err = f.ApiClient.GetEnvironment(context.Background(), opts.environmentID)
-			if err != nil {
-				return err
-			}
-		} else {
-			environments, err := f.ApiClient.ListEnvironments(context.Background(), projectID)
-			if err != nil {
-				return err
+			emitDeployDiagnostic("P2_DEPLOY_CREATE_SERVICE_PASS", "true")
+
+			if opts.environmentID != "" {
+				emitDeployDiagnostic("P2_DEPLOY_GRAPHQL_ENV_LOOKUP_SKIPPED", "true")
+				emitDeployDiagnostic("P2_DEPLOY_ENVIRONMENT_ID_EXPLICIT", opts.environmentID)
+				environment = explicitEnvironment(opts.environmentID, projectID)
+			} else {
+				emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
+				return fmt.Errorf("environment-id required when project-id is explicit")
 			}
-			if len(environments) == 0 {
-				return fmt.Errorf("no environment found")
-			}
-			environment = environments[0]
 		}
 	} else if opts.serviceID != "" {
-		// legacy non-interactive: --service-id without --project-id
 		service, err = f.ApiClient.GetService(context.Background(), opts.serviceID, "", "", "")
 		if err != nil {
+			emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
 			return err
 		}
 		projectID = service.Project.ID
 
 		if opts.environmentID != "" {
-			environment, err = f.ApiClient.GetEnvironment(context.Background(), opts.environmentID)
-			if err != nil {
-				return err
-			}
+			environment = explicitEnvironment(opts.environmentID, projectID)
+			emitDeployDiagnostic("P2_DEPLOY_GRAPHQL_ENV_LOOKUP_SKIPPED", "true")
+			emitDeployDiagnostic("P2_DEPLOY_ENVIRONMENT_ID_EXPLICIT", opts.environmentID)
 		} else {
 			environments, err := f.ApiClient.ListEnvironments(context.Background(), projectID)
 			if err != nil {
+				emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
 				return err
 			}
 			if len(environments) == 0 {
+				emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
 				return fmt.Errorf("no environment found")
 			}
 			environment = environments[0]
@@ -138,10 +164,15 @@ func runDeploy(f *cmdutil.Factory, opts *Options) error {
 	)
 	s.Start()
 
+	emitDeployDiagnostic("P2_DEPLOY_STAGE", "upload")
+	emitDeployDiagnostic("P2_DEPLOY_UPLOAD_STARTED", "true")
 	_, err = f.ApiClient.UploadZipToService(context.Background(), projectID, service.ID, environment.ID, bytes)
 	if err != nil {
+		emitDeployDiagnostic("P2_DEPLOY_UPLOAD_FAIL", "true")
+		emitDeployDiagnostic("P2_DEPLOY_UPLOAD_PASS", "false")
 		return err
 	}
+	emitDeployDiagnostic("P2_DEPLOY_UPLOAD_PASS", "true")
 	s.Stop()
 
 	domainName := opts.domainName
@@ -195,9 +226,6 @@ func selectInteractively(f *cmdutil.Factory, opts *Options) (*model.Service, *mo
 		spinner.WithSuffix(" Fetching projects ..."),
 	)
 	s.Start()
-	// Snapshot the active workspace so the hint below names the same team
-	// the create call actually files the project under. Reading the
-	// persisted workspace here would race the --workspace flag override.
 	ws := f.CurrentWorkspace()
 	ownerID := ws.ID
 	projects, err := f.ApiClient.ListAllProjects(context.Background(), ownerID)
@@ -212,9 +240,6 @@ func selectInteractively(f *cmdutil.Factory, opts *Options) (*model.Service, *mo
 			return nil, nil, err
 		}
 		if confirm {
-			// When the active workspace is a team, make it visible that the
-			// new project will land under that team — not the personal
-			// account — so the user isn't surprised by where it shows up.
 			if ws.IsTeam() {
 				f.Log.Infof("→ Creating new project in team workspace %q", ws.Name)
 			}
@@ -224,10 +249,6 @@ func selectInteractively(f *cmdutil.Factory, opts *Options) (*model.Service, *mo
 				return nil, nil, err
 			}
 			f.Log.Infof("Project %s created. Run this command again to deploy on it.", project.Name)
-			// `--workspace` override is one-shot (PLA-1590 B+): the newly-
-			// created project belongs to the override workspace, not to the
-			// persisted one, so writing it to the persisted context would
-			// silently cross workspaces on the next command. Skip.
 			if !f.HasWorkspaceOverride() {
 				f.Config.GetContext().SetProject(zcontext.NewBasicInfo(project.ID, project.Name))
 			}
@@ -272,7 +293,7 @@ func selectInteractively(f *cmdutil.Factory, opts *Options) (*model.Service, *mo
 		s = spinner.New(cmdutil.SpinnerCharSet, cmdutil.SpinnerInterval,
 			spinner.WithColor(cmdutil.SpinnerColor),
 			spinner.WithSuffix(" Creating new service ..."),
-		)
+	)
 		s.Start()
 
 		name := opts.name
